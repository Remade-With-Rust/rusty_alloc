//! Tier-B bench harness library (plan §7.2), the allocation trace format
//! (plan §7.3), the G1 replayer (plan §4), and the Tier-B kernels.

#![deny(missing_docs)]

pub mod kernels;
pub mod replay;
pub mod trace;
