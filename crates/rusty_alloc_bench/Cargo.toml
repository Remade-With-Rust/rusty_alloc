[package]
name = "rusty_alloc-bench"
description = "Tier-B native harness + allocation-trace record/replay + G1/G2 gates"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
# Not published: dev harness (Tier-B benches, trace record/replay, G1/G2 gates).
publish = false

[[bin]]
name = "rabench"
path = "src/main.rs"

[dependencies]
rusty_alloc = { path = "../rusty_alloc" }

[dev-dependencies]
rusty_alloc-api = { path = "../rusty_alloc_api" }
libloading = "0.8"

[lints]
workspace = true
