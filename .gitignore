/target
# The fuzz crate is a STANDALONE workspace, so the root-only `/target` rule
# above does not reach its build directory.
/fuzz/target/
/oracle/out/
/corpus/run/results/
*.ratrace
.DS_Store
/benchmark-*.txt
/.claude/
